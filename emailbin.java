import java.util.Arrays;
import java.util.ArrayList;
import java.util.List;
import java.util.HashMap;
public class emailbin{
    public static void main(String[]args){
        HashMap<String, Integer> mapsp = new HashMap<>();
        HashMap<String, Integer> maph = new HashMap<>();
        HashMap<String, Integer> mapavgsp = new HashMap<>();
        HashMap<String, Integer> mapavgh = new HashMap<>();
        int wsph = worddd.getword
        
        ArrayList<String> emlcmb = new ArrayList<>();

        // Arraylist<String> wordocc = n a l<>;
        // int emails = emailsct.length;
        // while hasnextline
        // for i in range {String[] honk = email[i].split(" "); forString se : honk) { emcomb.add honk[i]}
        // for i in range 0,emcomb.length{if map.containsemcomb[i] ? int ct = map.getemcomb[i] map.replace emcomb,ct++ : map.put(emcomb[i],1) wordocc.add(emcomb[i])}
        // hashmap str int avgmapsp = new hashmap; 
        // for i in range wordocc.length{int tot = map.get(wordocc[i]), double avg = tot/emails.length avgmap.put(wordocc[i],avg)  }

        // hashmap testhm = new hashmap
        // array wordoccc
        // emailstest =  the email  String[] words = emailstest.split(" ") for words.len if thm.contains(words[i]) ? int ct = thm.getwords[i] thm.replace words[i],ct++: thm.put words[i],1
        // for wordocc.length{int hamppint int spampp sppavgmapsp.get wordoccc int hamm = if havgmap .contains havgmap.get wordoccc else hampp ++ } 

        // int spsc = 0
        // int hsc = 0 

    }
}




